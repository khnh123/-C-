void MoveStrings(vector<string> &source, vector<string> &destination)
{
    for (int i=0; i<source.size(); i++) {  // go through vector
        destination.push_back(source[i]);
    }
    source.clear();
}
